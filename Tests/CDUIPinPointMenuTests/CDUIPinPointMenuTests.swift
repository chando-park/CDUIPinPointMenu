import XCTest
@testable import CDUIPinPointMenu

final class CDUIPinPointMenuTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CDUIPinPointMenu().text, "Hello, World!")
    }
}
